'use client';
import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {useSearchParams} from 'next/navigation';
export default function Reset(){const token=useSearchParams().get('token')||'';const [msg,setMsg]=useState('');const [err,setErr]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get('password')!==f.get('confirm'))return setErr('As senhas não conferem.');const r=await fetch('/api/auth/redefinir-senha',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password:f.get('password')})});const d=await r.json();if(!r.ok)return setErr(d.message);setMsg(d.message);setErr('');}return <main className="authPage"><form className="card" onSubmit={submit}><h2>Definir nova senha</h2><div className="sub">Crie uma senha segura com pelo menos 8 caracteres.</div>{err&&<div className="error">{err}</div>}{msg&&<div className="successBox">{msg} <Link href="/login">Ir para o login</Link></div>}<div className="field"><label>Nova senha</label><input name="password" type="password" minLength={8} required/></div><div className="field"><label>Confirmar senha</label><input name="confirm" type="password" minLength={8} required/></div><button className="btn">Salvar nova senha</button></form></main>}
