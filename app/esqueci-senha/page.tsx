'use client';
import Link from 'next/link'; import {FormEvent,useState} from 'react';
export default function Forgot(){const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/esqueci-senha',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email')})});const d=await r.json();setLoading(false);setMessage(d.message);} return <main className="authPage"><form className="card" onSubmit={submit}><Link className="back" href="/login">← Voltar ao login</Link><h2>Recuperar senha</h2><div className="sub">Informe seu e-mail para receber o link de redefinição.</div>{message&&<div className="successBox">{message}</div>}<div className="field"><label>E-mail</label><input name="email" type="email" required/></div><button className="btn" disabled={loading}>{loading?'Enviando...':'Enviar link de recuperação'}</button></form></main>}
