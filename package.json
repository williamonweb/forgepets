{
  "name": "forgepets",
  "version": "9.2.0",
  "private": true,
  "description": "ForgePets SaaS para pet shops, preparado para Vercel e Neon PostgreSQL.",
  "scripts": {
    "dev": "serve .",
    "vercel:dev": "vercel dev",
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^6.19.0",
    "vercel": "^48.0.0",
    "serve": "^14.2.4"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "engines": {
    "node": ">=20"
  }
}
